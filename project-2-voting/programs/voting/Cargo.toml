[package]
name = "voting"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "voting"

# [features]
# default = ["alloc"]

[dependencies]
pinocchio = "0.8.4"
pinocchio-log = "0.4.0"
borsh = { version = "=1.5.7", features = ["derive"] }