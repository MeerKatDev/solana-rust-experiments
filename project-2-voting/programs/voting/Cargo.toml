[package]
name = "voting"
version = "0.1.0"
description = "Using Pinocchio"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "voting"

# [features]
# default = ["alloc"]

[dependencies]
pinocchio = "0.8.4"
pinocchio-log = "0.4.0"
borsh = { version = "=1.5.7", features = ["derive"] }

[dev-dependencies]
mollusk-svm = "0.4.1"
getrandom = "0.3.3"
# pinocchio-pubkey = "0.2.1"